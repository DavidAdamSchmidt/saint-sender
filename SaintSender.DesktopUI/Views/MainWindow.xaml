<Window x:Class="SaintSender.DesktopUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SaintSender.DesktopUI.Views"
        xmlns:vm="clr-namespace:SaintSender.DesktopUI.ViewModels"
        xmlns:conv="clr-namespace:SaintSender.DesktopUI.Converters"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        mc:Ignorable="d"
        Title="MainWindow" ResizeMode="NoResize" Height="743.5" Width="899.5" >
    <Window.DataContext>
        <vm:MainWindowModel />
    </Window.DataContext>
    <Window.Resources>
        <conv:BooleanToFontWeightConverter x:Key="BoolToFont"/>
        <Style TargetType="local:MainWindow">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Height" Value="700"/>
            <Setter Property="Width" Value="850" />
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Width" Value="70" />
        </Style>
    </Window.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding ExitProgramCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Label Content="{Binding UserEmail}" Margin="0,74,189,0" VerticalAlignment="Top" HorizontalAlignment="Right" Height="40" VerticalContentAlignment="Stretch" Width="Auto" FontWeight="Bold"/>
        <Button x:Name="Logout" Command="{Binding LogoutButtonClickCommand}" CommandParameter="{Binding ElementName=Logout}" Margin="700,70,74,602">
            _Log Out
        </Button>
        <Button Command="{Binding SendNewButtonClickCommand}" Margin="74,69,700,603">
            _Send New
        </Button>
        <ListBox x:Name="Mails" ItemsSource="{Binding Emails}" Margin="74,154,74,107">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding ReadDoubleClickedEmail}" CommandParameter="{Binding ElementName=Mails, Path=SelectedItem}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel x:Name="Item">
                        <TextBlock Text="{Binding Path=Subject}" FontWeight="{Binding IsRead, Converter={StaticResource BoolToFont}}"/>
                        <TextBlock Text="{Binding Path=From}" FontWeight="{Binding IsRead, Converter={StaticResource BoolToFont}}"/>
                        <TextBlock Text="{Binding Path=Date}" FontWeight="{Binding IsRead, Converter={StaticResource BoolToFont}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="Previous" Margin="210,606,560,66" Width="74">
            _Previous
        </Button>
        <Button x:Name="Next" Margin="580,611,190,61" Width="74">
            _Next
        </Button>
        <Button Command="{Binding RefreshButtonClickCommand}" Margin="168,70,606,602">
            _Refresh
        </Button>
    </Grid>
</Window>
